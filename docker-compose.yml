version: "3.9"

services:

  studymate:

    build: .

    container_name: studymate

    ports:
      - "8501:8501"

    env_file:
      - .env

    volumes:
      - ./temp:/app/temp
      - ./vectordb:/app/vectordb

    restart: unless-stopped